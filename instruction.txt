A :

sudo yum update -y
sudo amazon-linux-extras enable epel
sudo yum install epel-release -y
sudo yum install ansible -y
ansible --version


B : 

Copy key

C :

Update IPs, than run : ansible mynginx -m ping -i myinventory.ini --extra-vars "@vars.yml"

D :

ansible-playbook -i myinventory.yml nginx_setup.yml --extra-vars "@vars.yml"
